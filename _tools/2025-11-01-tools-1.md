---
layout: single
title: "常用 GMT 绘图脚本示例"
excerpt: "GMT-Based Scripts for Common InSAR Visualization."
date: 2025-11-02
collection: tools
author_profile: true
categories: [GMT, GMTSAR使用]
---


数据可视化是科学研究中的关键环节，高质量的图件对于结果展示与分析至关重要。为提高 InSAR 后处理阶段的绘图效率并确保图件风格的统一性，本文展示了一些基于 Generic Mapping Tools (GMT) 的常用绘图脚本及数据格式转换工具。旨在为相关研究提供绘图参考。

**参考文档**：
在进行复杂绘图任务时，建议查阅 [GMT 中文手册](https://docs.gmt-china.org/) 以获取详细的参数说明与语法规范。

> **全局配置说明**：
> GMT 的绘图输出风格（包括字体设定、图幅尺寸、边框类型等）由一系列系统配置参数决定。为确保 InSAR 成果图件风格的统一性与规范化，推荐使用本团队 [gmt.conf](/code/gmt.conf) 配置文件
>
> 相关参考文献：关于参数配置的详细机制与修改方法，请参阅 [GMT中文手册 - 配置参数概览](https://docs.gmt-china.org/latest/conf/overview/)

---

## 1. 时空基线图绘制 (Spatiotemporal Baseline Visualization)

在完成干涉对的筛选与手动增删操作后，需重新绘制时空基线图以检查干涉网络的连通性与基线分布情况。

**脚本功能**：读取干涉对列表与基线表，绘制垂直基线与时间基线的分布关系。

**脚本调用命令：**

[plot_baseline.csh](/code/plot_baseline.csh)

```bash
plot_baseline.csh intf.in baseline_table.dat
```

**输出示例：**

<div align="center">
    <img src="/files/tools/baseline.png" width="80%" alt="时空基线图">
</div>

---

## 2. 雷达坐标系栅格数据可视化 (Cartesian/Radar Coordinates)

针对处于 SAR 原始几何（雷达坐标系）下的栅格数据（如相位图、相干性图），由于其尚未进行地理编码，需使用笛卡尔坐标系（Linear Projection）进行绘制以快速预览数据质量。

**参数说明**：
* `grd_file`: 输入的网格文件（雷达坐标系）
* `limitL/limitU`: 数据的显示范围下限/上限
* `cpt`: 颜色映射表 (Color Palette Table)

**脚本调用命令：**

[plot_grd_JX.csh](/code/plot_grd_JX.csh)

```bash
plot_grd_JX.csh grd_file limitL limitU cpt
```

**输出示例：**

<div align="center">
    <img src="/files/tools/plot_phase.jpg" width="80%" alt="时空基线图">
</div>


---

## 3. 地理坐标系栅格数据可视化 (Geographic/WGS84 Coordinates)

对于已完成地理编码（Geocoding）的栅格数据（如形变速率图、累积形变图），应使用地理坐标系绘图脚本，确保经纬度信息的正确投影展示（通常使用 Mercator 投影）。

**脚本调用命令：**

[plot_grd.csh](/code/plot_grd.csh)

```bash
plot_grd.csh grd_file limitL limitU
```

**输出示例：**

<div align="center">
    <img src="/files/tools/plot_vel.png" width="80%" alt="时空基线图">
</div>

---

## 4. 形变时间序列提取与绘图 (Time Series Plotting)

为直观展示地表形变的演化过程，需对提取的特征点（PS/DS点）进行时间序列绘制。该脚本支持自动读取时间序列文件并生成位移-时间曲线。

**脚本调用命令：**

[plot_timeseries.csh](/code/plot_timeseries.csh)

```bash
plot_timeseries.csh disp_file
```

**输出示例：**

<div align="center">
    <img src="/files/tools/plot_ts.png" width="80%" alt="时空基线图">
</div>


---

## 5. 数据格式转换协议 (Data Format Conversion)

在多源数据处理过程中，常需利用 GDAL/OGR 库进行数据格式的互转，以满足不同软件（如 ArcGIS, QGIS, GMT）的输入要求。

### 5.1 栅格数据转换 (Raster)

**TIFF 转 GMT NetCDF (GRD)：**
将 GeoTIFF 格式转换为 GMT 可识别的网格文件。

```bash
gdal_translate -of GMT Input.tif Output.grd
```

**GMT NetCDF (GRD) 转 GeoTIFF：**
将 GMT 处理后的结果导出为 GeoTIFF 以便在 GIS 软件中查看。

```bash
gdal_translate -of Gtiff Input.grd Output.tif
```

### 5.2 矢量数据转换 (Vector)

虽然 GMT 6.1 及以上版本已原生支持 ESRI Shapefile (`.shp`) 格式的直接读取与绘制，但在某些特定场景下，将 Shapefile 转换为 GMT 格式仍有助于简化文件管理。

**针对 GMT 6.5 及以上版本：**
[参考文档：GDAL/OGR 转换](https://docs.gmt-china.org/latest/utilities/gdal/#ogr2ogr/)

```bash
ogr2ogr -f "GMT" Output.gmt Input.shp
```

**针对 GMT 6.0 及早期版本：**
[参考文档：OGR2OGR 旧版支持](https://docs.gmt-china.org/6.0/dataset/gadm/?highlight=ogr2ogr)

```bash
ogr2ogr -f OGR_GMT Output.gmt Input.shp
```

<style>
    /* 1. 确保代码块容器有相对定位，以便按钮定位 */
    /* 覆盖 Minimal Mistakes 的默认样式 */
    .page__content .highlighter-rouge,
    .page__content .highlight,
    div.highlighter-rouge, 
    figure.highlight {
        position: relative !important;
    }

    /* 2. 复制按钮样式 */
    .copy-btn {
        position: absolute;
        top: 5px;
        right: 5px;
        height: 24px;
        padding: 0 8px;
        background-color: #fff;
        color: #555;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 12px;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        cursor: pointer;
        opacity: 0; /* 默认透明 */
        transition: opacity 0.2s, background-color 0.2s, color 0.2s;
        z-index: 999;
        line-height: 22px; /* 垂直居中文字 */
        outline: none;
    }

    /* 3. 鼠标悬停在代码块上时显示按钮 */
    .highlighter-rouge:hover .copy-btn,
    .highlight:hover .copy-btn {
        opacity: 1;
    }

    /* 4. 按钮悬停和点击效果 */
    .copy-btn:hover {
        background-color: #007bff; /* 悬停变为蓝色 */
        color: #fff;
        border-color: #007bff;
    }
    
    /* 复制成功后的状态 */
    .copy-btn.copied {
        background-color: #28a745; /* 成功变为绿色 */
        color: white;
        border-color: #28a745;
    }
</style>

<script>
document.addEventListener("DOMContentLoaded", function() {
    // 1. 定义选择器：针对 Minimal Mistakes 主题常见的代码块结构
    // 同时也尝试匹配通用的 pre 标签，以防万一
    const selectors = 'div.highlighter-rouge, figure.highlight, pre.highlight';
    const codeBlocks = document.querySelectorAll(selectors);

    codeBlocks.forEach(block => {
        // 如果这个元素是 pre，且它已经被包裹在 div.highlighter-rouge 里了，就跳过，防止重复添加
        if (block.tagName === 'PRE' && block.closest('div.highlighter-rouge')) {
            return;
        }

        // 检查是否已经有按钮
        if (block.querySelector('.copy-btn')) return;

        // 创建按钮
        const button = document.createElement('button');
        button.className = 'copy-btn';
        button.type = 'button';
        button.innerText = 'Copy';

        // 将按钮添加到代码块容器中
        // 注意：如果 position: relative 没生效，按钮可能会飞到别的地方
        block.appendChild(button);

        // 点击事件
        button.addEventListener('click', () => {
            // 获取代码文本
            // 优先找 code 标签，如果没有就找 block 文本
            const codeElement = block.querySelector('code');
            let codeText = codeElement ? codeElement.innerText : block.innerText;
            
            // 清理末尾多余的换行和按钮自己的文字（如果抓取范围过大的话）
            codeText = codeText.replace(/Copy$/, '');

            // 写入剪贴板
            navigator.clipboard.writeText(codeText).then(() => {
                button.innerText = 'Copied!';
                button.classList.add('copied');

                // 2秒后恢复原状
                setTimeout(() => {
                    button.innerText = 'Copy';
                    button.classList.remove('copied');
                }, 2000);
            }).catch(err => {
                console.error('Copy failed:', err);
                button.innerText = 'Error';
            });
        });
    });
});
</script>
